; Pest Browser (pest-plugin-browser): the Playwright WebSocket Client uses PHP assert() before sendText().
; zend.assertions cannot be set from phpunit.xml on many PHP builds ("only in php.ini"); this file is loaded
; by the Sail PHP CLI when mounted in compose.yaml.
zend.assertions = 1
assert.exception = 1
